[tools]
dotnet = "10.0.202"
node = "24"

[tasks.ci]
description = "Run the CI workflow locally with act."
dir = "."
run = "./scripts/act-ci.sh"

[tasks.cd]
description = "Run the CD workflow locally with act without publishing images or releases."
dir = "."
run = "./scripts/act-cd.sh"

[tasks.ui]
description = "Run frontend locally"
dir = "ui"
run = "npm run dev"

[tasks.be]
description = "Run backend locally"
dir = "be"
run = "dotnet run --project Cashregister.Api --configuration Debug"
env = { "ASPNETCORE_ENVIRONMENT" = "Production" }

[tasks.run]
description = "Run both frontend and backend locally"
run = [
  { tasks = ["be", "ui"] }
]

[tasks.compose]
description = "Run local docker-compose manifest"
run = "docker compose --file=docker-compose.local.yaml up --build --detach"