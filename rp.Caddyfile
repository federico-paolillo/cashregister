{
	admin off
	auto_https off
	persist_config off
	log {
		output stdout
		format console
	}
}

http://:65000 {
	log {
		output stdout
		format console
	}

	handle_path /api/* {
		reverse_proxy api:65000
	}

	handle {
		reverse_proxy ui:65000
	}
}
